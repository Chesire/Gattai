package com.chesire.gattai.provider.mapping

class SeriesIdMappingProvider {

    init {
        // Load the items into memory, maybe make an async function to do this
    }

    fun findByKitsuId(kitsuId: Int): SeriesIdMappingEntry? {
        // Find the entry by the kitsu id
        return null
    }

    fun findByMalId(malId: Int): SeriesIdMappingEntry? {
        // Find the entry by the mal id
        return null
    }

    fun findByAnilistId(anilistId: Int): SeriesIdMappingEntry? {
        // Find the entry by the anilist id
        return null
    }
}
